# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This is the initial stage of a website project for a bakery ("מאפיית לחם ושמש" — "Bread and Sun Bakery"). The repository currently contains only image assets in `תמונות/` (product photos and a logo/banner) — no site code, framework, or build tooling has been added yet. There are no build, lint, or test commands to run at this point.

When code is added, update this file with the actual build/lint/test commands and the resulting architecture.

## Notes for working in this repo

- File and folder names are in Hebrew and contain spaces (e.g. `תמונות/חלת שבת.jpg`). Always quote paths in shell commands.
- Git is initialized locally (no remote configured). Commit after every meaningful change with a message explaining what changed — don't batch unrelated changes into one commit, and don't wait until the end of a task to commit.
